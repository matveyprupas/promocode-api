# Copy to .env before running the app (see docker-compose.yml for DB container).
# After `docker compose up -d`, Prisma uses DATABASE_URL.

DATABASE_URL="postgresql://promocode:promocode_dev@localhost:5432/promocode?schema=public"
